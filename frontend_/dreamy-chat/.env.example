# Spring Boot backend that exposes the SockJS endpoint at `/chat`
# and STOMP destinations `/topic/message` (subscribe) and `/app/sendMessage` (publish).
VITE_BACKEND_URL=https://synctalk-production-8eba.up.railway.app